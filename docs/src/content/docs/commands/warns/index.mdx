---
title: Warns Commands
description: Complete guide to Warns module commands and features
---

import { Badge, Tabs, TabItem } from '@astrojs/starlight/components';

# Warns Commands

Keep your members in check with warnings — stop them getting out of control! If you're looking for automated warnings, read about the [Blacklists module](/commands/blacklists/).

<Tabs>
  <TabItem label="User Commands">
    ### User Commands <Badge text="Everyone" variant="success" />

    | Command | Description | Disableable |
    |---------|-------------|-------------|
    | `/warns` | See a user's warnings. | Yes |
  </TabItem>

  <TabItem label="Admin Commands">
    ### Admin Commands <Badge text="Admin Only" variant="caution" />

    | Command | Description | Disableable |
    |---------|-------------|-------------|
    | `/warn` | Warn a user. | No |
    | `/dwarn` | Warn a user by reply, and delete their message. | No |
    | `/swarn` | Silently warn a user, and delete your message. | No |
    | `/rmwarn` | Remove a user's latest warning. | No |
    | `/unwarn` | Remove a user's latest warning. Alias for /rmwarn. | No |
    | `/resetwarn` | Reset all of a user's warnings to 0. | No |
    | `/resetwarns` | Reset all of a user's warnings to 0. Alias for /resetwarn. | No |
    | `/warnings` | Get the chat's warning settings. | No |
    | `/setwarnmode` | Set the chat's warn mode. | No |
    | `/setwarnlimit` | Set the number of warnings before users are punished. | No |

    :::tip[Warn with Reason]
    ```text
    /warn @user For disobeying the rules
    /dwarn Spamming                          # Deletes their message too
    /swarn Inappropriate language             # Silently warns
    ```
    :::
  </TabItem>

  <TabItem label="Owner Commands">
    ### Owner Commands <Badge text="Owner Only" variant="danger" />

    | Command | Description | Disableable |
    |---------|-------------|-------------|
    | `/resetallwarns` | Delete all the warnings in a chat. All users return to 0 warns. | No |
  </TabItem>
</Tabs>

## Default Settings

| Setting | Default |
|---------|---------|
| Warn limit | 3 warnings |
| Warn mode | mute |

## Available Warn Modes

| Mode | Behavior |
|------|----------|
| `ban` | Permanently ban the user from the chat |
| `kick` | Kick the user (they can rejoin) |
| `mute` | Mute the user (cannot send messages) |

:::note
When a user reaches the warn limit, the configured action (ban/kick/mute) is applied automatically.
:::

## Module Aliases

This module can be accessed using the following aliases:
`warn`, `warning`, `warnings`

## Required Permissions

| Commands | Permission |
|----------|-----------|
| `/warn`, `/dwarn`, `/swarn` | Bot admin + User admin with restrict |
| `/rmwarn`, `/unwarn` | Bot admin + User admin |
| `/resetwarn`, `/resetwarns` | Bot admin + User admin |
| `/resetallwarns` | Chat owner only |
| `/setwarnmode`, `/setwarnlimit` | Bot admin + User admin |
| `/warnings` | Bot admin + User admin |
| `/warns` | Any user (disableable) |

## Technical Notes

- Warning reasons are stored and displayed when checking a user's warns
- The `/warns` command is the only command in this module that can be disabled by admins
- Anonymous channel posts cannot be warned
- Admins cannot be warned
